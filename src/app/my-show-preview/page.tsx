import PreviewExperience from "./PreviewExperience";

export const metadata = {
  title: "My Show preview · Savage Party",
  robots: { index: false, follow: false },
};

export default function MyShowPreviewPage() {
  return <PreviewExperience />;
}
