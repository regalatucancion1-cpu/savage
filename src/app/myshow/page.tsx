import MyShowExperience from "./PreviewExperience";

export const metadata = {
  title: "My Show · Savage Party",
  robots: { index: false, follow: false },
};

export default function MyShowPage() {
  return <MyShowExperience />;
}
