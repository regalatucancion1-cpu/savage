import MiShowExperience from "./PreviewExperience";

export const metadata = {
  title: "Mi Show · Savage Party",
  robots: { index: false, follow: false },
};

export default function MiShowPage() {
  return <MiShowExperience />;
}
